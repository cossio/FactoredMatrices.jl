var documenterSearchIndex = {"docs":
[{"category":"section","location":"reference/#Reference","page":"Reference","text":"","title":"Reference"},{"category":"module","location":"reference/#FactoredMatrices.FactoredMatrices","page":"Reference","text":"FactoredMatrices\n\nStore and operate on low-rank matrices in factored form A = u * v' without materializing the full dense matrix. The main type is FactoredMatrix.\n\nFor allocation-free repeated multiplications, create a FactoredMatrices.Workspace and pass it to mul! via the cache keyword, or bundle it with the matrix in a FactoredMatrices.CachedFactoredMatrix.\n\n\n\n\n\n","title":"FactoredMatrices.FactoredMatrices"},{"category":"type","location":"reference/#FactoredMatrices.CachedFactoredMatrix","page":"Reference","text":"CachedFactoredMatrix(M::FactoredMatrix, ws::Workspace)\nCachedFactoredMatrix(M::FactoredMatrix, p::Integer)\n\nBundle a FactoredMatrix with a pre-allocated FactoredMatrices.Workspace so they travel together through an API that takes a single matrix-like argument: products automatically use the bundled buffers as their mul! cache. The buffers serve only the products they fit, in shape (the Workspace carries a single outer size p) and element type; other products fall back to allocating. Use one CachedFactoredMatrix per task when multiplying concurrently.\n\nOnly multiplication-related operations are forwarded to the wrapped matrix (*, mul!, adjoint/transpose, scalar and UniformScaling multiples); for anything else, unwrap with C.M.\n\n\n\n\n\n","title":"FactoredMatrices.CachedFactoredMatrix"},{"category":"type","location":"reference/#FactoredMatrices.FactoredMatrix","page":"Reference","text":"FactoredMatrix(u, v')\n\nThe m × n matrix u * v' stored in factored form, where u is m × r and v is n × r (typically r < min(m, n)).\n\nThe second factor must be passed adjointed (or transposed), so that the call reads as the product it represents: FactoredMatrix(u, v') is the matrix u * v'. Passing two plain matrices throws an ArgumentError. Vectors are treated as one-column matrices, so FactoredMatrix(x, y') is the outer product of x and y. Structured matrices whose adjoint is eager (Diagonal, Hermitian, triangular, ...) are accepted directly and taken verbatim as the right multiplicand. Factors with different element types are promoted to a common element type.\n\nFactoredMatrix <: Factorization, so — like the standard-library Factorization types — it supports neither indexing nor iteration, and == compares the stored factors field-wise (use isapprox to compare the represented matrices). The supported operations (*, mul!, +, -, dot, norm, svd, \\, ...) exploit the factors and never materialize the dense m × n matrix.\n\nThe factors are stored (not copied) in the fields u (m × r) and v (n × r).\n\n\n\n\n\n","title":"FactoredMatrices.FactoredMatrix"},{"category":"type","location":"reference/#FactoredMatrices.Workspace","page":"Reference","text":"Workspace{T}(r::Integer, p::Integer)\nWorkspace(L::FactoredMatrix, p::Integer)\n\nPre-allocated scratch buffers for allocation-free mul! with a FactoredMatrix, passed via the cache keyword: mul!(C, L, B; cache = ws). Here r is the storage rank and p the outer size of the other operand:\n\nmul!(C, L, B; cache = ws) needs p = size(B, 2) (p = 1 for a vector B);\nmul!(C, A, L; cache = ws) needs p = size(A, 1);\nmul!(C, L, M; cache = ws) with both operands factored needs r = rank(L) and p = rank(M) (fully allocation-free when C is a FactoredMatrix).\n\nThe buffers are used only for products whose intermediates have exactly the element type T; other products fall back to allocating. Workspace(L, p) picks the element type of a same-element-type product of L (e.g. Int for Bool factors, whose products accumulate into Int). A Workspace can be reused across calls (including adjoint/transpose variants) as long as the operand sizes do not change; use one per task when multiplying concurrently. See also FactoredMatrices.CachedFactoredMatrix to bundle the buffers with the matrix.\n\n\n\n\n\n","title":"FactoredMatrices.Workspace"},{"category":"method","location":"reference/#Base.:+-Tuple{FactoredMatrices.FactoredMatrix, FactoredMatrices.FactoredMatrix}","page":"Reference","text":"+(A::FactoredMatrix, B::FactoredMatrix)\n-(A::FactoredMatrix, B::FactoredMatrix)\n\nLazy sum (difference) by concatenating the factors: the result is a FactoredMatrix of storage rank rank(A) + rank(B). The dense matrices are never formed.\n\n\n\n\n\n","title":"Base.:+"},{"category":"method","location":"reference/#Base.:\\-Tuple{FactoredMatrices.FactoredMatrix, AbstractVecOrMat}","page":"Reference","text":"\\(L::FactoredMatrix, b)\n\nMinimum-norm least-squares (pseudoinverse) solution of L * x = b, via svd(L) — the dense matrix is never materialized. Rank deficiency (including dependent factor columns from the lazy +/-) is handled with the same dimension-scaled singular-value cutoff as pinv.\n\n\n\n\n\n","title":"Base.:\\"},{"category":"method","location":"reference/#Base.isapprox-Tuple{FactoredMatrices.FactoredMatrix, FactoredMatrices.FactoredMatrix}","page":"Reference","text":"isapprox(A::FactoredMatrix, B::FactoredMatrix; atol = 0, rtol, nans = false)\n\nApproximate equality norm(A - B) ≤ max(atol, rtol * max(norm(A), norm(B))) in the Frobenius norm, like isapprox for arrays. A - B is itself low-rank, so its norm is evaluated from thin QR factorizations of the concatenated factors at O((m + n) * (rank(A) + rank(B))²) cost, never materializing the dense matrices. Mixed element types evaluate the distance entrywise (O(m * n * rank)), preserving each operand's represented-entry arithmetic, like dot. When the distance is not finite, falls back to elementwise comparison, like arrays.\n\n\n\n\n\n","title":"Base.isapprox"},{"category":"method","location":"reference/#LinearAlgebra.dot-Tuple{FactoredMatrices.FactoredMatrix, FactoredMatrices.FactoredMatrix}","page":"Reference","text":"dot(A::FactoredMatrix, B::FactoredMatrix)\n\nFrobenius inner product sum(conj(A[i, j]) * B[i, j]), evaluated in closed form from rank(A) × rank(B) Gram matrices at O((m + n) * rank(A) * rank(B)) cost, without materializing the dense matrices. Mixed element types are evaluated entrywise (O(m * n * rank)), preserving each operand's represented-entry arithmetic.\n\nThe closed form reassociates the sum, so relative accuracy degrades for strongly cancelling factorizations; self-inner products take the QR-stable sum(abs2, A) path instead.\n\n\n\n\n\n","title":"LinearAlgebra.dot"},{"category":"function","location":"reference/#LinearAlgebra.mul!","page":"Reference","text":"mul!(C, A, B, [α, β]; cache::Union{Nothing, FactoredMatrices.Workspace} = nothing)\n\nThree- and five-argument mul! where A, B and/or C are FactoredMatrixes, exploiting the factored form. Pass a pre-allocated FactoredMatrices.Workspace as cache to make repeated products allocation-free.\n\n\n\n\n\n","title":"LinearAlgebra.mul!"},{"category":"function","location":"reference/#LinearAlgebra.norm","page":"Reference","text":"norm(A::FactoredMatrix, p = 2)\n\nFrobenius norm of the represented matrix, computed from the factors at O((m + n) * rank²) cost without materializing the dense matrix. Only p = 2 is supported.\n\nBy unitary invariance ‖u * v'‖ = ‖R_u * R_v'‖ with R_u, R_v the triangular QR factors. Unlike the Gram form tr((u'u)(v'v)), this does not cancel catastrophically when the represented matrix is much smaller than its factors (e.g. A - B of two nearly-equal matrices), which is what makes isapprox reliable.\n\n\n\n\n\n","title":"LinearAlgebra.norm"},{"category":"method","location":"reference/#LinearAlgebra.rank-Tuple{FactoredMatrices.FactoredMatrix}","page":"Reference","text":"rank(L::FactoredMatrix)\n\nThe storage rank of the factorization: the number of columns of the factors. This is an upper bound of (not necessarily equal to) the numerical rank of the represented matrix.\n\n\n\n\n\n","title":"LinearAlgebra.rank"},{"category":"method","location":"reference/#LinearAlgebra.svd-Tuple{FactoredMatrices.FactoredMatrix}","page":"Reference","text":"svd(A::FactoredMatrix)\n\nSingular value decomposition from QRs of the factors and an SVD of the small min(m, r) × min(n, r) core — cheaper than svd(Matrix(A)) when r < m, n.\n\nReturns a reduced LinearAlgebra.SVD with min(m, n, r) singular values (U is m × k, Vt is k × n); the singular values omitted relative to svd(Matrix(A)) are all zero.\n\n\n\n\n\n","title":"LinearAlgebra.svd"},{"category":"section","location":"#FactoredMatrices.jl","page":"Home","text":"A Julia package implementing a FactoredMatrix type: a lazy matrix stored as a product u * v' of two factor matrices, u (of size m × r) and v (of size n × r), typically with r < m, n. Storing the factors instead of the full m × n matrix saves memory and makes many operations cheaper when the rank r is small.\n\nFactoredMatrix is a LinearAlgebra.Factorization, not an AbstractMatrix: it does not support iteration or the dense generic fallbacks, and instead implements *, mul!, +, -, scalar multiples, dot, norm, tr, svd, \\ (least-squares / pseudoinverse solution), isapprox, and conversion via Matrix(A) — all exploiting the factored form.","title":"FactoredMatrices.jl"},{"category":"section","location":"#Installation","page":"Home","text":"This package is not registered. Install with:\n\nimport Pkg\nPkg.add(url = \"https://github.com/cossio/FactoredMatrices.jl\")","title":"Installation"},{"category":"section","location":"#Quick-start","page":"Home","text":"The second factor is passed adjointed, so the constructor call reads as the product it represents:\n\nusing LinearAlgebra\nusing FactoredMatrices\n\nu = randn(100, 5)\nv = randn(50, 5)\nA = FactoredMatrix(u, v') # lazy representation of u * v', of size 100 x 50\n\nsize(A) # (100, 50)\nrank(A) # 5 (storage rank: the number of factor columns)\n\nx = randn(50)\nA * x # efficient: never forms the full 100 x 50 matrix\n\nB = A' * A # products of factored matrices stay factored\nC = A + A  # lazy sums by factor concatenation (storage rank 10)\nA ≈ C - A  # closed-form Frobenius distance, no dense matrix is built\nF = svd(A) # SVD exploiting the factored form\n\nMatrix(A) # materialize the full dense matrix\n\nOperations such as scalar multiplication, matrix products, adjoint/transpose, and svd exploit the factored representation and return FactoredMatrix results where possible, without materializing the full dense matrix.","title":"Quick start"},{"category":"section","location":"#Allocation-free-repeated-multiplication","page":"Home","text":"For tight loops, pre-allocate a FactoredMatrices.Workspace and pass it via the cache keyword of mul!:\n\nB = randn(50, 7)\nC = zeros(100, 7)\nws = FactoredMatrices.Workspace(A, size(B, 2))\n\nmul!(C, A, B; cache = ws)           # no allocation\nmul!(C, A, B, 2.0, 1.0; cache = ws) # five-argument form, also supported\n\nOr bundle the buffers with the matrix in a FactoredMatrices.CachedFactoredMatrix, so any generic code calling * or mul! uses them automatically.\n\nSee the Reference section for the full API documentation.","title":"Allocation-free repeated multiplication"},{"category":"section","location":"#Related-packages","page":"Home","text":"HolyLab/FactoredMatrices.jl\nLowRankApprox.jl","title":"Related packages"}]
}
